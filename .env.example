# Optional — only needed for the "AI summary" narrative button on the
# Data profile page. The app works fully without it (rule-based summary
# is shown instead).
OPENROUTER_API_KEY=
AGENTIC_DS_MODEL=openai/gpt-4o-mini
